// Euclidean algorithm (GCD) in e2
// Usage: ./e2peg examples/gcd.e2 <a> <b>

a := arg1
b := arg2

loop {
  case {
    a == 0 -> break
    b == 0 -> break
    a == b -> break
    a > b  -> a := a - b
    1      -> b := b - a
  }
}

case {
  a == 0 -> print b
  1      -> print a
}
