// Test: Runtime error prevents parsing of subsequent invalid syntax
//
// e1peg: Executes statement, hits runtime error, stops - never parses invalid code
// e1: Parses all first, then executes - but many(pstmt) stops at invalid syntax anyway

print 1
break_ifz 0
@@@ invalid
